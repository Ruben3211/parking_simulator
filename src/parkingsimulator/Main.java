package parkingsimulator;

public class Main {
	
	/**
	 * This method creates a new simulator object from the Simulator class. It then
	 * calls on the run() method in the simulator class to run the application.
	 * 
	 * @param args Unused
	 */
	public static void main(String[] args) {
		Simulator simulator = new Simulator();
		simulator.run();
	}
}
